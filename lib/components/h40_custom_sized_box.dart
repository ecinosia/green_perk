import 'package:flutter/material.dart';

class H40CustomSizedBox extends StatelessWidget {
  const H40CustomSizedBox({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 40,
    );
  }
}
