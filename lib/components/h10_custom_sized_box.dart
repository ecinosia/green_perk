import 'package:flutter/material.dart';

class H10CustomSizedBox extends StatelessWidget {
  const H10CustomSizedBox({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}
